
namespace Loye.Proxy
{
    public enum ListenerType
    {
        Http = 0,
        Sock,
    }

    public enum ProxyType
    {
        Http = 0,
        Sock,
    }

    public enum ProviderType
    {
        Direct = 0,
        GoogleEngine,
    }
}
